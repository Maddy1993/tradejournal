{
  "http.port": 8080,
  "db.host": "${DB_HOST:localhost}",
  "db.port": ${DB_PORT:5432},
  "db.database": "${DB_NAME:tradejournal}",
  "db.user": "${DB_USER:postgres}",
  "db.password": "${DB_PASSWORD:postgres}",
  "db.pool.max_size": 10
}
